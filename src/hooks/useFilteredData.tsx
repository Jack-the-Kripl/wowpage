// import { useCallback, useRef, useState } from "react";

// export default function useFilteredData() {
// 	const defaultData = useRef<Array<any>>([]);
// 	const [filteredData, setFilteredData] = useState<Array<any>>([]);

// 	const filterData = useCallback((filter: object) => {
		
// 	}, [setFilteredData, defaultData.current]);

// 	const setData = useCallback((data: Array<any>) => {
// 		defaultData.current = data;
// 	}, []);
// }